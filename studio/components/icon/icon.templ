package icon

type Props struct {
	Class string
	Size  int
}

templ Database(props ...Props) {
	{{ s := 16; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<ellipse cx="12" cy="5" rx="9" ry="3"></ellipse>
		<path d="M3 5V19A9 3 0 0 0 21 19V5"></path>
		<path d="M3 12A9 3 0 0 0 21 12"></path>
	</svg>
}

templ Table(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="M12 3v18"></path>
		<rect width="18" height="18" x="3" y="3" rx="2"></rect>
		<path d="M3 9h18"></path>
		<path d="M3 15h18"></path>
	</svg>
}

templ Enum(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<line x1="8" y1="6" x2="21" y2="6"></line>
		<line x1="8" y1="12" x2="21" y2="12"></line>
		<line x1="8" y1="18" x2="21" y2="18"></line>
		<line x1="3" y1="6" x2="3.01" y2="6"></line>
		<line x1="3" y1="12" x2="3.01" y2="12"></line>
		<line x1="3" y1="18" x2="3.01" y2="18"></line>
	</svg>
}

templ Scalar(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<polyline points="16 18 22 12 16 6"></polyline>
		<polyline points="8 6 2 12 8 18"></polyline>
	</svg>
}

templ Policy(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
	</svg>
}

templ Trigger(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"></polygon>
	</svg>
}

templ Extension(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
		<polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline>
		<line x1="12" y1="22.08" x2="12" y2="12"></line>
	</svg>
}

templ Function(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="M18 10h-4v4"></path>
		<path d="M6 14h4v-4"></path>
		<path d="M14 6v8a4 4 0 0 1-4 4H6"></path>
		<path d="M10 18V10a4 4 0 0 1 4-4h4"></path>
	</svg>
}

templ Query(props ...Props) {
	{{ s := 15; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
		<polyline points="14 2 14 8 20 8"></polyline>
		<line x1="16" y1="13" x2="8" y2="13"></line>
		<line x1="16" y1="17" x2="8" y2="17"></line>
		<polyline points="10 9 9 9 8 9"></polyline>
	</svg>
}

templ Key(props ...Props) {
	{{ s := 12; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="m15.5 7.5 2.3 2.3a1 1 0 0 0 1.4 0l2.1-2.1a1 1 0 0 0 0-1.4L21 4"></path>
		<path d="m21 2-9.6 9.6"></path>
		<circle cx="7.5" cy="15.5" r="5.5"></circle>
	</svg>
}

templ Link(props ...Props) {
	{{ s := 12; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"></path>
		<path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"></path>
	</svg>
}

templ Sun(props ...Props) {
	{{ s := 16; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<circle cx="12" cy="12" r="4"></circle>
		<path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"></path>
	</svg>
}

templ Play(props ...Props) {
	{{ s := 14; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="currentColor" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<polygon points="6 3 20 12 6 21 6 3"></polygon>
	</svg>
}

templ Search(props ...Props) {
	{{ s := 14; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<circle cx="11" cy="11" r="8"></circle>
		<path d="m21 21-4.3-4.3"></path>
	</svg>
}

templ ChevronLeft(props ...Props) {
	{{ s := 16; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="m15 18-6-6 6-6"></path>
	</svg>
}

templ ChevronRight(props ...Props) {
	{{ s := 16; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="m9 18 6-6-6-6"></path>
	</svg>
}

templ ChevronDown(props ...Props) {
	{{ s := 14; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<path d="m6 9 6 6 6-6"></path>
	</svg>
}

templ Plus(props ...Props) {
	{{ s := 14; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<line x1="12" y1="5" x2="12" y2="19"></line>
		<line x1="5" y1="12" x2="19" y2="12"></line>
	</svg>
}

templ Trash(props ...Props) {
	{{ s := 14; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<polyline points="3 6 5 6 21 6"></polyline>
		<path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
	</svg>
}

templ Code(props ...Props) {
	{{ s := 14; if len(props) > 0 && props[0].Size > 0 { s = props[0].Size } }}
	<svg xmlns="http://www.w3.org/2000/svg" width={ string(rune(s)) } height={ string(rune(s)) } viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class={ propsClass(props...) }>
		<polyline points="16 18 22 12 16 6"></polyline>
		<polyline points="8 6 2 12 8 18"></polyline>
	</svg>
}

func propsClass(props ...Props) string {
	if len(props) > 0 {
		return props[0].Class
	}
	return ""
}
