# Changelog

## 0.0.1

- Initial release: syntax highlighting, comment toggling, and bracket matching for
  **Axel Schema** (`.asl`) and **Axel Query** (`.aql`), including AQL `$name<type>`
  parameters, `@name` / `@request` / `@response` directives, and the shape `*` splat.
