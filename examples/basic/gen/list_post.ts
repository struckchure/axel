// Code generated by axel codegen --generator ts. DO NOT EDIT.

import type { DB } from "./runner.ts";

export interface ListPostRow {
  id: string;
  title: string;
  author: ListPostRowAuthor;
}

export interface ListPostRowAuthor {
  id: string;
  name: string | null;
}

export async function listPost(db: DB): Promise<ListPostRow[]> {
  const query = `SELECT
  p.id AS id,
  p.title AS title,
  (SELECT row_to_json(u_author_sub) FROM (SELECT u_author.id AS id, u_author.name AS name FROM "user" u_author WHERE u_author.id = p.author LIMIT 1) u_author_sub) AS author
FROM "post" p;`;
  return db.unsafe<ListPostRow>(query);
}
