// Code generated by axel codegen --generator ts. DO NOT EDIT.

declare const _axelRelation: unique symbol;
/** A multi-link field: target rows, present only when selected. */
export type Relation<T> = T[] & { readonly [_axelRelation]: true };
/** The keys of T that are relations rather than columns. */
export type RelationKeys<T> = {
  [K in keyof T]-?: NonNullable<T[K]> extends { readonly [_axelRelation]: true } ? K : never;
}[keyof T];
/** true when T is a relation field type. */
export type IsRelation<T> = NonNullable<T> extends { readonly [_axelRelation]: true } ? true : false;
/** The row type behind a relation field: Relation<User> → User[]. */
export type RelationRows<T> = NonNullable<T> extends Array<infer U> ? U[] : never;

export interface Comment {
  content: string;
  createdAt: Date;
  id: string;
  updatedAt: Date;
  authorId: string;
  postId: string;
}

export interface Post {
  content: string;
  createdAt: Date;
  id: string;
  slug?: string | null;
  title: string;
  updatedAt: Date;
  authorId: string;
  likes?: Relation<User>;
}

export interface User {
  active?: boolean | null;
  age: number;
  createdAt: Date;
  email: string;
  health: number;
  id: string;
  name?: string | null;
  updatedAt: Date;
}

